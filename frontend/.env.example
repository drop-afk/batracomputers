# For local development — Vite proxy handles /api routing, so leave blank
# VITE_API_URL=

# For production deployment (Vercel/Netlify) — set this to your backend URL
# VITE_API_URL=https://your-backend.railway.app
